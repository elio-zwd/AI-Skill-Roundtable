# 模型选择 Bottom Sheet 候选

> 图片类型：模型与 API Key 页上的 Modal Bottom Sheet
>
> 输出：1 张 1440 × 3200 Android 竖屏图片

## 1. Sheet

遵循[共享视觉基线](../共享/jianyu-shared-visual-baseline.md)。背景为模型与 API Key 页，35% Scrim。

- 标题：`选择文本模型`
- 副说明：`可用选项取决于你配置的服务与 API Key`
- 右上关闭；
- 底部按钮：`取消` / `确认选择`。

## 2. 模型供应商卡

### Gemini

- 当前选中；
- 蓝色 Check；
- 说明：`支持对话、资料理解和联网能力`
- 状态：`已配置 2 个 Key`

### DeepSeek

- 未选中；
- 说明：`可用于文本对话`
- 状态：`尚未配置 Key`

如果后续工程核对不支持某供应商，最终规格必须删除该项；本图只用于比较选择界面结构。

## 3. 视觉与边界

- 不显示模型排行榜和价格；
- 不展示 temperature、top-p 等工程参数；
- 未配置 Key 的选项不自动跳过用户确认；
- 不承诺所有功能对每个模型都可用。
