# PR09：见域生产实施任务清单

> 路线图：[`pr-09-jianyu-implementation-plan.md`](./pr-09-jianyu-implementation-plan.md)
>
> 规格入口：[`pr-08f-final-spec-index.md`](./pr-08f-final-spec-index.md)
>
> 本清单按实际执行顺序排列；PR09-01～18 是能力域编号，02、10、13、14 已拆为原子子阶段。

## 1. 统一启动门禁

每个 PR09 实施 PR 开始前：

- [ ] 另一个 AI 已对 PR08-F 修正 Head 完成严格只读复核；
- [ ] 用户已在复核后批准 PR08-F，且 PR08-F 已合并；
- [ ] 从 GitHub 重新记录最新 `main` SHA；
- [ ] 从该 SHA 创建独立分支和 Draft PR；
- [ ] 读取适用的 `AGENTS.md`、最终规格入口和上游交接；
- [ ] 检查开放 PR、近期提交、共享文件和不可并行域；
- [ ] 建立文件级可执行计划并通过只读复核；
- [ ] 计划列出准确文件、接口、失败测试、命令、提交边界和回滚；
- [ ] 为隐私、Key、遥测、敏感上下文和高风险行为建立本阶段门禁；
- [ ] 未经用户授权不标记 Ready、不合并、不删除分支；
- [ ] 不把未执行的测试写成通过。

视觉授权边界：

- [x] 用户已授权在上述门禁满足后先实施 PR09-01～15；
- [ ] PR09-16 启动前必须取得最终视觉选择；
- [ ] PR09-17 必须等待 PR09-16；
- [ ] PR09-18 的仓库、官网、DNS、签名与发布动作必须逐项另行授权。

## 2. PR09-01 应用身份

建议分支：`refactor/pr-09-01-jianyu-app-identity`

- [ ] 迁移 `applicationId`、`namespace`、源码和测试包路径；
- [ ] 同步 Manifest、Provider、资源引用、Room Schema 输出、CI、R8 和脚本；
- [ ] 保留候选无关的临时图标，不提交最终视觉资产；
- [ ] 先添加旧包数据/Key 不可见、旧引用残留和构建路径失败测试；
- [ ] 验证 Debug / Release、单测、Lint、Instrumentation 和安装启动；
- [ ] 验证新旧包并存、新包空沙箱、Key 重新配置；
- [ ] 回滚点为整个身份 PR，不设计旧包数据回退。

## 3. PR09-02A 核心领域 Schema

建议分支：`feat/pr-09-02a-core-domain-schema`

- [ ] 建立 Issue、Stage、Message、ExecutionRun 和参与者快照关系；
- [ ] 先添加 `roundIndex != Stage`、重复 Run、孤儿关系和进程恢复失败测试；
- [ ] 冻结最小 DAO 契约、外键、索引、事务和 Schema 导出；
- [ ] 不加入资料、成果、音频、背景、组合或回收站；
- [ ] 验证初始 Schema，不设计旧包跨应用 Migration；
- [ ] 记录进入长期测试后的 Migration 纪律和回滚点。

## 4. PR09-02B 资源与生命周期 Schema

建议分支：`feat/pr-09-02b-resource-lifecycle-schema`

- [ ] 建立资料引用、背景使用、总结草稿、成果、音频引用；
- [ ] 建立官方 Skill 组合、归档和回收站元数据；
- [ ] 组合可保存官方成员、顺序和可选默认职责；
- [ ] 默认职责不得改写官方 Skill 正文、系统边界或安全规则；
- [ ] 先添加草稿自动过期、历史被改写、自定义 Skill 混入和孤儿资产失败测试；
- [ ] 验证彻底清除边界可表达，但不实现业务清理事务；
- [ ] 只回滚 02B，不回退已被消费的 02A。

## 5. PR09-03 Repository 与恢复

建议分支：`feat/pr-09-03-jianyu-repository-recovery`

- [ ] 仅保存议题与开始运行分离；
- [ ] 创建/撤销阶段、保存/放弃草稿、确认成果；
- [ ] 归档/恢复、关联议题、回收站/彻底清除事务；
- [ ] 运行状态、部分成功和重试幂等恢复；
- [ ] 先添加重复点击、事务中断、数据库异常、附件缺失和进程重建测试；
- [ ] 禁止其他阶段直接 DAO 调用形成第二套业务路径；
- [ ] 回滚 Repository 适配层时保留 Schema。

## 6. PR09-04 导航壳

建议分支：`feat/pr-09-04-jianyu-navigation-shell`

- [ ] 建立首页、议题、Skill、资料与成果、设置次级入口；
- [ ] 建立资料/成果 Tab、归档/回收站入口、深链和返回栈；
- [ ] 页面只提供占位状态，不触发业务写入；
- [ ] 新 `testTag` 采用稳定语义命名，保留旧标签一个稳定验收周期；
- [ ] 先添加错误 Tab、返回栈恢复、旧标签改义和占位写入失败测试；
- [ ] 独占根导航；与 05 并行时提供预留 Route 接口。

## 7. PR09-05 Skill 目录

建议分支：`feat/pr-09-05-official-skill-catalog`

- [ ] 建立四类 Skill、来源许可、风险时效、发布和批次状态；
- [ ] 44 项候选状态真实，20 项人物 Skill 全量可发现且不因风险降权；
- [ ] 建立最近使用、收藏和官方组合；
- [ ] 组合支持可选默认职责，但不开放自定义 Skill；
- [ ] “去AI化助手”所有用户入口就近显示诚信边界；
- [ ] 先添加风险降权、未发布可执行、声明缺失和边界缺失测试；
- [ ] 不修改根导航、Database 或 Repository 写事务。

04 与 05 只有在 03 合并并记录独占文件后才可有限并行。

## 8. PR09-07 执行运行

建议分支：`feat/pr-09-07-execution-run`

- [ ] 显式 ExecutionRun；
- [ ] 单/多 Skill、顺序执行、预算、流式、停止、部分/全部失败；
- [ ] 原位重试不重复成功成员；
- [ ] 进程恢复且 `roundIndex` 只表示响应批次；
- [ ] 先添加停止后写入、重复预算、全员重试、占位悬挂和日志泄密测试；
- [ ] 若使用旧调度兼容层，记录真实调用方、测试和删除阶段；
- [ ] 独占执行状态机，禁止并行修改。

## 9. PR09-09 资料与个人背景

建议分支：`feat/pr-09-09-material-context-source`

- [ ] 资料库、议题/阶段关联、授权、来源日期和联网条件；
- [ ] 背景库、议题显式选择、本次实际使用记录；
- [ ] 默认不勾选，长期信息只在用户主动确认后保存；
- [ ] 编辑、停用、归档、普通删除、彻底清除和匿名占位；
- [ ] 先添加静默全量传递、历史改写、敏感占位和错误升级测试；
- [ ] 独占上下文组装接口；06、08 不得自行拼接全量背景。

## 10. PR09-06 首页与推荐

建议分支：`feat/pr-09-06-home-recommendation`

- [ ] 问题输入、现实支持/思维拓展双方向卡、跳过/单选/组合；
- [ ] 获取建议、理由、风险、时效、联网和阵容调整；
- [ ] 资料与个人背景确认；
- [ ] 保存议题但不推荐 Skill、不调用模型、不创建 Run；
- [ ] 用户最终确认前不运行；
- [ ] 先添加取消仍运行、重复创建、双主线和推荐失败丢草稿测试；
- [ ] 消费 04、05、07、09 接口，不依赖 10A / 10B，也不实现平行状态机。

## 11. PR09-08 点名与交叉讨论

建议分支：`feat/pr-09-08-directed-cross-discussion`

- [ ] 消费 05、06、07、09；通过 06 已确认议题的工作区入口接入；
- [ ] 临时点名和显式交叉讨论均默认单次生效；
- [ ] 选择参与者与焦点，传递最小上下文并记录实际使用内容；
- [ ] 收束保留共识、分歧、条件、建议和下一步，不投票裁决；
- [ ] 先添加永久模式、越权上下文、参与者漂移和重试变更测试；
- [ ] “去AI化助手”不得自动取得其他成员全部输出；
- [ ] 回滚时关闭入口但保留历史消息。

## 12. PR09-10A 草稿与成果

建议分支：`feat/pr-09-10a-draft-result`

- [ ] 阶段总结草稿跨 Activity / 进程持久化且不自动过期；
- [ ] 默认通用总结，经用户确认后转为正式成果；
- [ ] 无成果直接推进；
- [ ] 成果来源链、修订关系和删除边界；
- [ ] 通过 06 的议题工作区集成点接入，不依赖 08 的交叉讨论行为；
- [ ] 导出文件不作为成果类型；
- [ ] 先添加恢复自动升级、保存失败丢草稿、放弃误删和推进被阻断测试；
- [ ] 独占成果确认事务。

## 13. PR09-10B 音频资产

建议分支：`feat/pr-09-10b-audio-assets`

- [ ] 音频作为独立可关联资产；
- [ ] 建立来源、任务、生命周期和受控清理，并向 12 提供删除、恢复和清理接口；
- [ ] 音频失败不影响文本成果；
- [ ] 先添加缺失、取消、空间不足、重复生成和孤儿文件测试；
- [ ] 不重写 10A 成果状态机；
- [ ] 回滚时关闭入口并保留可识别元数据。

## 14. PR09-11 推进议题

建议分支：`feat/pr-09-11-advance-issue`

- [ ] 正式入口始终可用；
- [ ] 三步确认，最终确认前不创建 Stage；
- [ ] 双方向组合仍只创建单一阶段；
- [ ] 无成果可推进，未运行阶段可无倒计时撤销；
- [ ] 旧阶段完整保存；
- [ ] 先添加预建节点、重复创建、误删上一阶段、运行后撤销和正式“下一轮”残留测试；
- [ ] 独占阶段创建入口。

## 15. PR09-12 归档与回收站

建议分支：`feat/pr-09-12-archive-trash`

- [ ] 只由用户主动归档；
- [ ] 运行中不预选，由用户选择等待完成或停止生成；
- [ ] 恢复继续记录“现在有什么变化”，或创建关联新议题；
- [ ] 普通删除进入无自动过期回收站；
- [ ] 容量不足只提醒，彻底清除展示范围并二次确认；
- [ ] 消费 10B 音频/附件生命周期接口，不重写音频状态机；
- [ ] 验证删除、恢复和彻底清除不会遗留音频、附件或后台清理任务；
- [ ] 先添加后台仍运行、永久丢失、自动清空、音频/附件清理失败、任务竞争和半删除测试；
- [ ] 独占删除与清理入口。

## 16. PR09-13A 备份安全设计

建议分支：`security/pr-09-13a-backup-design`

- [ ] 建立威胁模型、Envelope、版本和密钥生命周期候选；
- [ ] 专项选择 KDF / AEAD 与参数，不从 PR08 直接推断；
- [ ] 建立公开测试向量和错误语义；
- [ ] 先添加错误密码、篡改、降级、截断、超大输入和版本失败测试；
- [ ] 验证密保不能重置/解密旧备份，改密码不覆盖唯一旧备份；
- [ ] 只做设计、评审和原型测试，不写生产导出实现；
- [ ] 评审未通过不得进入 13B。

## 17. PR09-13B 加密导出与快照

建议分支：`feat/pr-09-13b-encrypted-export-snapshot`

- [ ] 导出始终加密并使用独立备份密码；
- [ ] 新备份自动校验，可选隔离验证；
- [ ] 设备绑定快照、手动备注、回退前快照和容量提示；
- [ ] 快照不含备份密码/应用锁凭据，导出默认排除快照；
- [ ] 创建后和恢复前验证；
- [ ] 先添加空间不足、进程终止、损坏快照、密钥不可用和附件不完整测试；
- [ ] 回滚不得删除用户已有备份或快照。

## 18. PR09-14A 隔离导入与差异预览

建议分支：`feat/pr-09-14a-isolated-import-preview`

- [ ] 临时只读隔离区；
- [ ] 格式、版本、完整性和安全校验；
- [ ] 分类汇总后逐项差异；
- [ ] 相同数据去重，冲突可选当前/备份/两者；
- [ ] 先添加路径穿越、压缩炸弹、格式混淆、超大附件和损坏索引测试；
- [ ] 预览和取消不得写当前库；
- [ ] 回滚只删除临时隔离区。

## 19. PR09-14B 原子替换与回退

建议分支：`feat/pr-09-14b-atomic-import-replacement`

- [ ] 替换前创建并验证恢复快照；
- [ ] 合并/替换保持 Room、WAL/SHM、附件和后台任务一致；
- [ ] 每个切换点做断电/进程终止故障注入；
- [ ] 失败不修改当前库，成功后无混合版本；
- [ ] 回退前再创建并验证快照；
- [ ] 原子算法在本阶段计划中评审选择，不预设 rename 或受控重启；
- [ ] 绝对禁止与数据库、附件或后台任务修改并行。

## 20. PR09-15 隐私与风险终审

建议分支：`fix/pr-09-15-privacy-risk-closure`

- [ ] 审计 01～14B 的日志、遥测、网络、导出和测试数据流；
- [ ] 使用 Canary 验证 Key、密码、背景和敏感正文不泄露；
- [ ] 验证遥测同意/关闭、来源时效和高风险响应；
- [ ] 验证人物风险不降权；
- [ ] 验证“去AI化助手”全部入口的诚信边界；
- [ ] 缺陷使用独立原子修复提交，不借机重构；
- [ ] 终审失败阻止 16～18。

## 21. PR09-16 品牌视觉实现

建议分支：`design/pr-09-16-jianyu-ui-system`

启动前：

- [ ] 用户明确选择候选 0 / A / B / C 或混合方案；
- [ ] 最终 Logo / App Icon 方向已记录；
- [ ] 商标、字体和商店相似度至少完成初步核验。

实现：

- [ ] Compose Token、Logo、App Icon、启动页、关于页和通知图标；
- [ ] 只修改主题、资产、布局适配和无障碍修饰，不重写业务状态机；
- [ ] 明暗主题、360 dp、200% 字号、TalkBack 和减少动效；
- [ ] 先添加候选混用、对比度、截断、主题恢复和仅靠颜色表达测试；
- [ ] 回滚主题/资产时保留稳定业务 UI。

## 22. PR09-17 端到端回归

建议分支：`test/pr-09-17-jianyu-e2e-release-gates`

- [ ] 等待 01～16 全部合并；
- [ ] 核心路径、进程重建、大数据量、数据库/附件异常；
- [ ] 离线、无 Key、限流、服务失败和导入故障注入；
- [ ] 性能、内存、无障碍、明暗主题和窄屏；
- [ ] Secret scan、Release APK、真机/模拟器和回滚演练；
- [ ] 缺陷回到责任域独立修复，不在测试提交中夹带重构；
- [ ] 输出发布门禁报告。

## 23. PR09-18 仓库、官网与发布

建议分支：`release/pr-09-18-jianyu-public-launch`

- [ ] 仓库改名获得单独授权；
- [ ] `origin`、README、Badge、Release 和外部链接获得授权；
- [ ] 官网、DNS、HTTPS、隐私、条款、文档和下载页获得授权；
- [ ] 正式签名、发布渠道和应用商店信息获得授权；
- [ ] 先验证旧链接、证书、下载校验、签名和政策页失败场景；
- [ ] 记录每项外部状态的恢复步骤；
- [ ] 不与核心代码重构并行。

## 24. 每个任务的完成输出

每个 PR 必须输出：仓库/PR、Base/Head/分支/提交、净文件范围、完成内容、失败测试、实际验证命令和退出码、CI、未验证项、风险、回滚、兼容层状态、下一阶段接口，以及本地 AI 只读验收 Prompt。
