# UI-02 Skill 角色页面状态

> 更新：2026-09-11
>
> 分支：`codex/ui-02-role-spec`
>
> PR：#59（Draft）

## 当前目标

把【角色】一级页面从错误的“旧 20 个 Character”前提修正为当前 **44 项 OfficialSkillCatalog**，并在不破坏已选 A/B 视觉的前提下完成角色发现分类、全屏详情与真实对话接入。

## 已完成设计与实现

- [x] A/B 主页面视觉方向已选定。
- [x] UI-02 双布局规格已更新为 v0.3 并审查。
- [x] 当前官方 Catalog / execution Manifest 复核为 44 项正式角色资产。
- [x] R1：44 项全部作为可发现 Skill 角色进入【角色】目录。
- [x] R2：发现分类为 `思考方法 / 职业成长 / 研究学习 / 产品创造 / 沟通表达 / 办公事务 / 生活工具`，加 `全部` 共 8 个 Chip。
- [x] R3：固定 `推荐角色` 为 **纳瓦尔 / 理查德·费曼 / 纳西姆·塔勒布**，不得使用 `为你推荐`。
- [x] R4：真人复用稳定人物头像；功能型角色使用稳定非真人身份视觉。
- [x] 44 项 Presentation Manifest + 严格 Parser/Loader 已实现。
- [x] 44 项不可变角色 UiState / projection 已实现；旧 Character 不再决定目录基数。
- [x] 共享 `JianyuRoleAvatar` 已实现，并由 Dialog 薄适配层复用。
- [x] A/B 统一角色主页面已实现：搜索、8 Chips、收藏、筛选、推荐、真实 recent、全部角色、分类双列/安全降单列与空状态。
- [x] 一级【角色】入口已切到专用 `SkillRoleCatalogRoute`；旧 Catalog/组合能力保留兼容但不再作为新一级页面。
- [x] 真正二级全屏 `SkillRoleDetailRoute/Screen` 已实现；正文滚动、底部双动作固定、系统 Insets 已处理；打开详情不写 recent。
- [x] `OfficialSkillConversationRoleAdapter` 已实现；第 21～44 项可按真实使用动作按需 materialize 为 legacy Character。
- [x] `createNewSessionWithSkillRole` / `addSkillRoleToCurrentSessionAwait` 已实现；修复 create-then-add 与 session 切换竞态。
- [x] App 仅在上述 Boolean 动作成功后写 `recordSkillUsed`。
- [x] 已增加 JVM / Compose / Android asset+Room / ViewModel 会话动作测试。
- [x] 本地 AI 只读验收 Prompt 已更新：`docs/testing/ui-02-role-page-local-readonly-acceptance.md`。

## 当前 Plan

`docs/superpowers/plans/2026-09-10-ui-02-role-page.md`

状态：**Task 1～6 代码实现完成；Task 7 验证中。**

关键架构：

```text
OfficialSkillCatalog（44，权威）
+ SkillRolePresentationCatalog（分类/编辑精选）
→ 角色页 UiState
→ 用户真正开始/加入对话时
→ OfficialSkillConversationRoleAdapter 按需生成/更新 legacy Character
→ participantIds
→ 现有 RoundtableOrchestrator / SkillLoader
```

不会把 `skills_config.json` 扩写成新的 44 项权威目录，也不会在浏览角色页时批量写 Character。

## Task 状态

- [x] Task 1 — 建立 44 项角色展示事实源（代码完成；JVM 本地验证待执行）
- [x] Task 2 — 构建 44 项不可变角色页 UiState（代码完成；JVM 本地验证待执行）
- [x] Task 3 — 建立人物头像与功能角色稳定身份视觉（代码完成；Instrumentation 受基线阻塞）
- [x] Task 4 — 实现 A/B 统一角色主页面（代码完成；真机视觉待验收）
- [x] Task 5 — 实现全屏 Skill 角色详情（代码完成；真机视觉待验收）
- [x] Task 6 — 打通 44 项到真实对话的按需兼容链路（代码完成；本地功能验收待执行）
- [ ] Task 7 — 回归、共享导航依赖与本地 UI 验收（进行中）

## 已取得远端验证证据

- [x] Secret scan：PASS。
- [x] `Android UI Test Compile` 已真实执行到 `:app:compileDebugKotlin`，**生产 Kotlin 编译 PASS**。
- [x] 第一轮暴露 UI-02 的不可用 Material Icons，已最小修复且未新增依赖。
- [x] 第二轮暴露 UI-02 新测试的无效 Compose assertion imports，已修复且没有删除/弱化断言。
- [x] 最新一轮再次通过 `:app:compileDebugKotlin`；`compileDebugAndroidTestKotlin` 中 **UI-02 新增测试已无编译错误**。
- [ ] 聚合 `assembleDebugAndroidTest`：被 `main` 已存在的 `IssueExecutionStopAvailabilityTest.kt:49` 旧 `IssueExecutionBudgetUi` 构造参数阻塞，不能作为 UI-02 PASS，也不归因于 UI-02。
- [ ] 主 Android CI：仍在历史 `tools/check-app-identity.ps1` identity gate 先行失败，Gradle unit/lint/assemble 被跳过。

## 尚待本地 AI 验收

- 精确 UI-02 JVM 测试；
- `compileDebugKotlin` 本地复核；
- 全量 `testDebugUnitTest`；
- `lintDebug`；
- `assembleDebug`；
- `assembleDebugAndroidTest`：预期可能仅被已知基线测试阻塞，禁止本任务修改该测试；
- 若 AndroidTest APK 因基线阻塞无法生成，则对应 Instrumentation 标记 `BLOCKED_BASELINE`，不通过删/改旧测试绕过；
- Xiaomi 14 Ultra 1440×3200 对照两张选定 PNG 的视觉验收；
- 第 21～44 项功能角色实际“开始新对话 / 增加到当前会话”手工验证；
- recent-use 只在真实成功动作后写入的手工验证。

## UI-01 外部依赖

当前 `main` 仍未落地最终共享 `对话 / 角色 / 资料 / 我的` Bottom Navigation。UI-02 没有私建第二套底栏；运行时旧 `首页 / 议题 / Skill / 资料与成果` 壳层应记录为 **UI-01 dependency**，不是 UI-02 回归。

## PR 状态

PR #59 保持 Draft、未合并、不可在本地验收完成前宣称 UI-02 完成；除非用户明确要求，不自动合并。
