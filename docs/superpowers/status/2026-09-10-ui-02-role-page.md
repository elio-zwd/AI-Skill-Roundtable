# UI-02 Skill 角色页面状态

> 更新：2026-09-12
>
> 分支：`codex/ui-02-role-spec`
>
> PR：#59（Draft）
>
> UI-02 本轮代码/测试验收 HEAD：`575840f626b7b508fe6522d0271141b8deff5610`
>
> 注：本轮包含两处主题背景语义修复与目录测试选择器修正；未修改 Room、导航产品结构或角色会话业务逻辑。

## 当前目标

把【角色】一级页面从错误的“旧 20 个 Character”前提修正为当前 **44 项 OfficialSkillCatalog**，并在不破坏已选 A/B 视觉的前提下完成角色发现分类、全屏详情与真实对话接入。

## 当前阶段

**UI-02 主体实现与 Task 7 最终门禁已完成，PR #59 已达到 merge-ready 条件。**

本轮结论：

- UI-01 shared Root Navigation（`对话 / 角色 / 资料 / 我的`）仍是下一独立批次；UI-02 没有复制第二套 BottomNavigation；
- PR #60 已合入 main（merge commit `e742008b38603ccb1d531e1972adb44e3037971b`），AndroidTest fixture 基线已恢复；
- PR #59 当前 Head 的 Android UI Test Compile 与 secret scan 通过；Android CI 仅失败于已确认的历史 `check-app-identity.ps1` 身份门禁；
- 本地/AGY Gradle 与 UI-02 定向 instrumentation 均通过，H-1/H-2/H-3 评审已完成。

## 已完成设计与实现

- [x] A/B 主页面视觉方向已选定。
- [x] UI-02 双布局规格已更新为 v0.3 并审查。
- [x] 当前官方 Catalog / execution Manifest 复核为 44 项正式角色资产。
- [x] R1：44 项全部作为可发现 Skill 角色进入【角色】目录。
- [x] R2：发现分类为 `思考方法 / 职业成长 / 研究学习 / 产品创造 / 沟通表达 / 办公事务 / 生活工具`，加 `全部` 共 8 个 Chip。
- [x] R3：固定 `推荐角色` 为 **纳瓦尔 / 理查德·费曼 / 纳西姆·塔勒布**，不得使用 `为你推荐`。
- [x] R4：真人复用稳定人物头像；功能型角色使用稳定非真人身份视觉。
- [x] 44 项 Presentation Manifest + 严格 Parser/Loader 已实现。
- [x] 44 项不可变角色 UiState / projection 已实现；旧 Character 不再决定目录基数。
- [x] 共享 `JianyuRoleAvatar` 已实现，并由 Dialog 薄适配层复用。
- [x] UI-02 专用 `SkillRolePageScreen` 已实现，并根据 Drive 原图完成视觉校准。
- [x] A 页大 Hero + 两张次级推荐卡已通过本地视觉验收。
- [x] B 页双列大身份视觉与分类说明卡已实现；`生活工具` 最终运行时滚动证据确认 5/5 可达。
- [x] 全屏 `SkillRoleDetailRoute/Screen` 已实现并完成视觉校准。
- [x] raw `career_workplace / creator_business / personal_finance` 等内部 tag 已从主页面与详情隐藏。
- [x] `OfficialSkillConversationRoleAdapter` 已实现。
- [x] `createNewSessionWithSkillRole` / `addSkillRoleToCurrentSessionAwait` 已实现。
- [x] App 代码仅在上述 Boolean 动作成功后写 `recordSkillUsed`。
- [x] 已增加 JVM / Compose / Android asset+Room / ViewModel 会话动作测试。

## 当前 Plan

`docs/superpowers/plans/2026-09-10-ui-02-role-page.md`

状态：**Task 1～7 已完成；UI-01 作为下一独立批次继续。**

关键架构：

```text
OfficialSkillCatalog（44，权威）
+ SkillRolePresentationCatalog（分类/编辑精选）
→ 角色页 UiState
→ 用户真正开始/加入对话时
→ OfficialSkillConversationRoleAdapter 按需生成/更新 legacy Character
→ participantIds
→ 现有 RoundtableOrchestrator / SkillLoader
```

不会把 `skills_config.json` 扩写成新的 44 项权威目录，也不会在浏览角色页时批量写 Character。

## Task 状态

- [x] Task 1 — 建立 44 项角色展示事实源。
- [x] Task 2 — 构建 44 项不可变角色页 UiState。
- [x] Task 3 — 建立人物头像与功能角色稳定身份视觉。
- [x] Task 4 — 实现 A/B 统一角色主页面；A/B 当前验收通过，LIFE_TOOLS 5/5 可达。
- [x] Task 5 — 实现全屏 Skill 角色详情；视觉通过。
- [x] Task 6 — 44 项到真实对话的按需兼容链路完成；Start New / Add Current / participant / recent-use 已获得可归因运行时证据。
- [x] Task 7 — 回归、依赖边界与本地 UI 验收。UI-01 依赖已记录且未复制底栏；PR #60 已恢复 AndroidTest 编译基线；本地/AGY/远端定向证据已收口。

## 后续收口 / 第三轮最终状态（2026-09-12）

以下为当前状态，覆盖后文“第二轮本地验收”中的历史失败结论：

- [x] **P0 Start New bridge**：PASS。真实进入新 session，并能确认 `meeting-to-action` participant。
- [x] **P0 Add Current bridge**：PASS。保持 captured current session，participant 增加成功。
- [x] **recent-use**：PASS。成功 use action 后可归因刷新；不再处于“未验证”状态。
- [x] **R-03 LIFE_TOOLS**：PASS。批准的 5 项全部存在；第三行滚动后可见第 5 项 `culture-fortune-entertainment`。
- [x] **R-05 model call**：PASS。`meeting-to-action → 详情 → 开始新对话 → hello → 真实回复` 链路完成。
- [x] `compileDebugKotlin`：PASS。
- [x] targeted JVM：PASS。
- [x] `testDebugUnitTest`：PASS。
- [x] `assembleDebug`：PASS。

### R-03 澄清

早期 `生活工具` “4/5”不是 Catalog / Presentation Manifest / projection 丢项，也不是需要补 Manifest 的生产缺陷。

- `skill_role_presentation_v1.json` 从始至终包含批准的 5 项；
- `projectSkillRoleCatalog(... selectedCategory=LIFE_TOOLS)` 的精确断言固定为 5 个批准 ID；
- B 页为双列网格，5 项形成三行；
- 第二轮截图只覆盖首屏视口，前两行四项可见，第五项位于第三行、视口下方；
- 后续实际向下滚动后已确认第三行 `culture-fortune-entertainment` 可达。

因此早期 4/5 结论正式归类为**首屏视口误判**，不再作为当前 Bug。

## 已关闭项与下一批次边界

### 1. UI-01 shared Root Navigation

最终共享 `对话 / 角色 / 资料 / 我的` Bottom Navigation 仍属于 UI-01。UI-02 已保持边界，不私建第二套底栏；这不是 UI-02 的剩余 blocker。

### 2. AndroidTest baseline

PR #60 已将 `IssueExecutionStopAvailabilityTest.kt` 的 fixture 更新为当前两字段生产契约，`assembleDebugAndroidTest` 与 Android UI Test Compile 已通过。主 Android CI 的四项身份门禁失败仍是独立历史基线，不归因于 UI-02。

### 3. H review

- H-1：角色目录页、详情页和详情操作栏使用 `MaterialTheme.colorScheme.background`，移除页面私有硬编码背景色；
- H-2：新角色发现/详情入口与旧 Official Skill 组合编辑/数据能力职责分离，旧能力保留；
- H-3：成功/失败返回、当前 session 捕获、participant 写入和 recent-use 条件已核查，本轮没有复现错会话或半完成状态，未扩大生产逻辑。

### 4. 下一批次

UI-01 仅在 UI-02 合入 main 后开始，沿用已批准的 Mine B 规格；本 PR 不提前实现 UI-01。

## 最终验证证据（2026-09-12）

- 本地 `compileDebugKotlin`、`testDebugUnitTest`、`lintDebug`、`assembleDebug`、`assembleDebugAndroidTest`：PASS。
- AGY `jianyu_compile_test`、`jianyu_lint_assemble`：均返回 exit code 0，且未修改源码。
- `emulator-5554` 定向 instrumentation：`JianyuRoleAvatarTest` 3/3、`SkillRoleCatalogScreenTest` 4/4、`SkillRoleDetailScreenTest` 2/2、`OfficialSkillConversationRoleAdapterAndroidTest` 1/1、`RoundtableViewModelSkillRoleActionsTest` 1/1、`AppNavHostTest` 8/8：PASS。
- 全量 `connectedDebugAndroidTest` 未作为 UI-02 通过依据：既有数据库/身份/旧导航测试出现失败，并在既有 execution 测试处停滞后主动中止；未发现本次 UI-02 主题或角色链路相关的新失败。
- PR #59 当前 Head：Android UI Test Compile PASS、secret scan PASS；Android CI 仍只失败于历史 `check-app-identity.ps1` 的四项身份门禁。

## 第二轮本地验收（2026-09-11，历史快照，原文保留）

> 本节保留当时的失败原文，仅用于审计历史；**不代表当前状态**。当前状态以上文“后续收口 / 第三轮最终状态”为准。

验收 HEAD：`7599755022448bf365411a94568d7963cdc48bd4`

设备：Android Emulator `1080x2400`，density 420，font scale 1.0。

Drive run：

`https://drive.google.com/drive/folders/1dW5L7-grPB00N4l1iMgAKpN-l5W0rcr4`

结果：

```text
COMPILE_DEBUG_KOTLIN: PASS
JVM_TARGETED: PASS
LINT_DEBUG: PASS
ASSEMBLE_DEBUG: PASS
ASSEMBLE_DEBUG_ANDROID_TEST: BLOCKED_BASELINE
VISUAL_A_PAGE: PASS
VISUAL_B_PAGE: FAIL
VISUAL_DETAIL: PASS
RAW_DOMAIN_TAGS_HIDDEN: PASS
MANUAL_START_NEW_BRIDGE: FAIL
MANUAL_ADD_CURRENT_BRIDGE: FAIL
RECENT_USE_SEMANTICS: FAIL
MODEL_CALL: NOT_RUN
```

### 已证实剩余问题

1. `生活工具` 第二轮首屏 4/5 已由 R-03 调查定位为滚动视口证据误判；数据/projection 为精确 5 项，最终 HEAD 仍需补第三行滚动截图/UI dump 作为运行时收口证据。
2. `meeting-to-action` 点击 `开始新对话` 后 10 秒仍停在详情页，Dialog participant 无法证明。
3. `study-planner` 点击 `增加到当前会话` 后仍停留详情页，双 participant 无法证明。
4. recent 已有历史状态，而两条 use action 本轮均未完成，recent-use 语义无法归因验证。
5. 因未进入 Dialog，本轮 `MODEL_CALL: NOT_RUN`。

## PR 状态

PR #59：**Open + Draft，内容已达到 merge-ready 条件，待执行合入**。

当前规则：

- Android CI 的历史身份门禁失败单独记录，不作为 UI-02 代码失败；
- 不宣称 UI-01 已完成；
- UI-02 merge 后再进入唯一的 UI-01 分支与 Draft PR。

下一步执行 PR #59 的 Ready/merge；合入后读取最新 main，创建唯一 UI-01 分支并按已批准 Mine B 规格实施。
