name: Secret scan

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: secret-scan-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Print PR09-08 branch tree SHA
        shell: pwsh
        run: Write-Host "PR09-08_BRANCH_TREE_SHA=$(git rev-parse "origin/$env:GITHUB_HEAD_REF`^{tree}")"

      - name: Scan tracked files and reachable history
        shell: pwsh
        run: ./tools/check-secrets.ps1 -IncludeHistory